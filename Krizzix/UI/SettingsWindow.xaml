<Window x:Class="Krizzix.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Krizzix Settings"
        Width="760"
        Height="560"
        MinWidth="640"
        MinHeight="480"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        Foreground="#D4D4D4"
        FontFamily="Segoe UI"
        FontSize="13"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBrush" Color="#1E1E1E" />
        <SolidColorBrush x:Key="PanelBrush" Color="#252526" />
        <SolidColorBrush x:Key="CardBrush" Color="#252526" />
        <SolidColorBrush x:Key="InputBrush" Color="#1B1B1C" />
        <SolidColorBrush x:Key="BorderBrush" Color="#3C3C3C" />
        <SolidColorBrush x:Key="TextBrush" Color="#D4D4D4" />
        <SolidColorBrush x:Key="MutedBrush" Color="#9DA0A6" />
        <SolidColorBrush x:Key="AccentBrush" Color="#0E639C" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#1177BB" />
        <SolidColorBrush x:Key="ButtonBrush" Color="#2D2D30" />
        <SolidColorBrush x:Key="ButtonHoverBrush" Color="#37373D" />
        <SolidColorBrush x:Key="ButtonPressedBrush" Color="#3F3F46" />
        <SolidColorBrush x:Key="ErrorBrush" Color="#A1260D" />
        <SolidColorBrush x:Key="ErrorPanelBrush" Color="#3A1C14" />
        <SolidColorBrush x:Key="ErrorTextBrush" Color="#F2C8BF" />
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="CaretBrush" Value="{StaticResource TextBrush}" />
            <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Margin" Value="0,6,0,0" />
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ButtonBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="MinWidth" Value="96" />
            <Setter Property="Padding" Value="16,8" />
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ButtonHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource ButtonPressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentHoverBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource AccentHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBrush}" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="20" Margin="0,0,0,18">
            <StackPanel>
                <TextBlock Text="Krizzix" FontSize="26" FontWeight="SemiBold" />
                <TextBlock Text="Headless window hider configuration. Mirrors the Suterusu startup model: run silently by default, open this window with --open-settings." Foreground="{StaticResource MutedBrush}" Margin="0,6,0,0" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource CardBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="Executables To Hide" FontSize="16" FontWeight="SemiBold" />
                    <TextBlock Text="One executable per line. Use names like rustdesk.exe, notepad.exe, calc.exe. Matching is case-insensitive." Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" />
                </StackPanel>

                <TextBox x:Name="TxtExecutables" Grid.Row="1" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="14" />

                <StackPanel Grid.Row="2" Margin="0,16,0,0">
                    <CheckBox x:Name="ChkPartialMatch" Content="Use partial matching" />
                    <TextBlock Text="Partial matching helps with targets like RustDesk windows when process names differ slightly from the configured value." Foreground="{StaticResource MutedBrush}" Margin="24,2,0,0" />
                </StackPanel>

                <Grid Grid.Row="3" Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Polling interval (ms)" VerticalAlignment="Center" FontWeight="SemiBold" />
                    <TextBox x:Name="TxtPollingInterval" Grid.Column="1" />
                    <TextBlock Grid.Column="2" Text="Lower values hide new windows faster; 100 ms is the default." Foreground="{StaticResource MutedBrush}" Margin="12,0,0,0" VerticalAlignment="Center" />
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Border x:Name="ValidationBar" Grid.Column="0" Background="{StaticResource ErrorPanelBrush}" BorderBrush="{StaticResource ErrorBrush}" BorderThickness="1" Padding="12" Visibility="Collapsed">
                <TextBlock x:Name="TxtValidation" Foreground="{StaticResource ErrorTextBrush}" />
            </Border>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Click="OnCancel" Style="{StaticResource SecondaryButtonStyle}" />
                <Button Content="Save" Click="OnSave" FontWeight="SemiBold" Style="{StaticResource PrimaryButtonStyle}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
